# 운영 환경 설정
server:
  tomcat:
    threads:
      max: 100
      min-spare: 10
    accept-count: 100

spring:
  application:
    name: flash-deal

  datasource:
    url: jdbc:mysql://${MYSQL_HOST}/${MYSQL_DBNAME}?serverTimezone=Asia/Seoul&characterEncoding=UTF-8&useUnicode=true
    username: ${MYSQL_USERNAME}
    password: ${MYSQL_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 10000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: false

toss:
  base-url: ${TOSS_BASE_URL}
  secret-key: ${TOSS_SECRET_KEY}

ncp:
  object-storage:
    endpoint: https://kr.object.ncloudstorage.com
    region: kr-standard
    access-key: ${NCP_ACCESS_KEY}
    secret-key: ${NCP_SECRET_KEY}
    bucket: ${NCP_BUCKET:flash-deal-images}
    default-image-url: ${NCP_DEFAULT_IMAGE_URL}

scheduling:
  enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  endpoint:
    prometheus:
      enabled: true
  metrics:
    tags:
      application: flash-deal
    export:
      prometheus:
        enabled: true